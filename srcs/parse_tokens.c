#include "../includes/minishell.h"

void	create_cmd_list(t_token *token_list)
{
	//percorrer a lista e, mediante o tipo de token, criar a linked list de comandos
	//guardar tudo ate ao | num node e a seguir noutro, ou sem nenhum pipe, guardar tudo
	//num so node
}

